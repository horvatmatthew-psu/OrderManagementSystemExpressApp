apiVersion: v1
kind: Service
metadata:
  name: order-management-service
  namespace: my-app
spec:
  selector:
    app: order-management-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer